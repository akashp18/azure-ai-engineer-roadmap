# Week 2 Practice: Object-Oriented Programming (OOP)
# Goal: Create a class that acts as a blueprint for a User Profile.

# TODO 1: Define a class named `UserProfile`.
# Write your class definition below:



    # TODO 2: Create the `__init__` method. It should take `self`, `name`, and `email` as arguments.
    # Inside the method, set `self.name = name` and `self.email = email`.
    # Also set `self.is_active = True`.
    # Write your __init__ method below:
    
    
    
    # TODO 3: Create a method named `deactivate`. It should take `self` as an argument.
    # Inside the method, set `self.is_active = False` and print a message saying the user was deactivated.
    # Write your method below:
    
    

# TODO 4: Outside the class, create an instance (object) of `UserProfile`.
# Pass in your name and email. Save it to a variable called `my_profile`.
# Write your code below:



# TODO 5: Print `my_profile.name` and `my_profile.is_active`.
# Then call `my_profile.deactivate()` and print `my_profile.is_active` again.
# Write your code below:

